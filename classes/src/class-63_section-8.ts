
// Vamos analisar o exemplo a seguir:

const date = new Date();

/*
    Por exemplo, as datas em JavaScript na API de data em si são orientadas a objetos, então 
    se você quiser instanciar uma data que corresponda à instância atual no tempo, neste 
    momento você precisa chamar um construtor usando a palavra-chave new.

    ------> ou seja, estou usando new, estou chamando um construtor

    E como podemos ver, a data é uma classe (classe Date()), então estamos usando o construtor 
    de data para instanciar um objeto, que é uma instância da classe, e a classe mistura os 
    dados que são relevantes para uma data, neste caso o timestamp Unix epoch no tempo.
    obs.: milessegundos desde a época Unix

    Quando estamos trabalhando com uma classe já presente no TypeScript, temos vários métodos 
    associados a mesma que podemos utilizar. Para isso, digitamos o nome da variável e um ponto,
    então os métodos associados irão aparecer para escolha

    date.nome_do_método
*/

/*
    
*/

